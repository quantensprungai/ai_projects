<!--
Reality Block
last_update: 2026-09-04
scope: Phasen-Plan 2 — self_identity vertikal
in_scope: Kanten, Resonanz, Stimme, eine Route
out_of_scope: Mandala-SVG, 12 Seiten, Cross-Kanten, Werkstatt-Seite, Re-Synth
-->

# Plan 02 — self_identity

Erst nach Phasen-Review 1 bauen. Details: Cursor-Plan und [vertraege/](../vertraege/).

## Todos

- *(D strukturell / S Schwelle)* `life-domains.ts`; `ic_seed_structure.py`: 12 `ic.life_domain.*`; Kanten strukturell `approved` (Ziwei/Astro/HD-OS) und inhaltlich `candidate` aus `payload.life_domain` (Dry-Run zuerst, nur HD)
- *(D)* HD-Wirkungskanten der Login-Person lesen → [kanten.md](../vertraege/kanten.md)
- *(D)* Migration `ic_resonance` + RLS + Server Action; Onboarding persistieren
- *(S)* `handbook-voice.ts` neben `hd-handbook-gloss.ts`
- *(S Gate / D Umsetzung)* Route `/home/karte/bereich/[domain]` nur `self_identity`; `domain-assemble.ts` Status-Gate (`language` + Allowlist); Filter-Chips; Leerfall; Werkstatt-Tor aus `experiment_seed`; Hub-Einstieg; `hdSnippet`-Leak weg
- *(D)* Browser Login-Person; dann 3–5 echte Charts qualitativ
- *(S)* Phasen-Review 2 → Plan 03 (BaZi)

## Erwartetes Ergebnis

HD-Karte echt (Keil), Astro/Ziwei als Lage-Sätze mit Entwurfs-Hinweis, ein Werkstatt-Satz. Das beziffert die Arbeit pro Domäne.

## Nicht

Zwölf Seiten, Mandala-SVG, Cross-Kanten, Re-Synth, DE-Atome-Welle, `tag_ic_metadata`, `extract_relationships`, Assemble der System-Charts umbauen, `supabase db reset`, Merge `main`.
