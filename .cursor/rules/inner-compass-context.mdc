---
description: Inner Compass — Leitplanken. Anker ist die Roadmap, nicht diese Rule.
globs: projects/inner_compass/**/*,code/inner_compass_app/**/*
alwaysApply: false
---

# Inner Compass

Zuerst lesen: `projects/inner_compass/cursor/roadmap.md`, dann den dort verlinkten Phasen-Plan. Warum: `cursor/nordstern.md`. Verträge: `cursor/vertraege/`.

## Leitplanken (Schaden verhindern)

- Antworte auf Deutsch.
- Docs-Repo ≠ Code-Repo. Nested `code/inner_compass_app/` nicht ins Docs-Repo committen.
- Kein `supabase db reset` (KG lebt lokal).
- Kein Force-Push, kein Merge `main` in diesem Slice.
- Kein Auto-Synth. Kein Spark-Qwen als Interpret oder Synth. Langdock bleibt Standard.
- Nicht committen: `.env*`, `_tmp_*`, Flora/ESF, PDFs in `scripts/`.
- HD-Zombie-Job `5ba2f841` nicht anfassen.

## Nicht hier

Keine Pipeline-Leseliste, keine Chart-Anker, keine Verbotsliste über die Leitplanken hinaus. Das steht in Roadmap, Verträgen und Decisions.
